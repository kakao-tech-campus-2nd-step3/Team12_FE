import AttendDateInput from '@/features/attend/AttendDateInput';
import { DefaultPaddedContainer } from '@/components/container/variants';

export default function AttendDateCreation() {
  return (
    <DefaultPaddedContainer>
      <AttendDateInput />
    </DefaultPaddedContainer>
  );
}
